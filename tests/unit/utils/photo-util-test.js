import photoUtil from '../../../utils/photo-util';
import { module, test } from 'qunit';

module('Unit | Utility | photo util');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = photoUtil();
  assert.ok(result);
});
