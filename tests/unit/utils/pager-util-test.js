import pagerUtil from '../../../utils/pager-util';
import { module, test } from 'qunit';

module('Unit | Utility | pager util');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = pagerUtil();
  assert.ok(result);
});
